---
id: legacy-active-0030
date: 2026-08-27
recorded_date: 2026-08-27
date_basis: heading
imported: 2026-09-06
kind: source
status: historical
tags: ["historical", "x11", "policy"]
---
# 2026-08-27: a stale xmonad proposal is not a policy-process crash

Historical source, not a current status claim. <a href="../../../history/research-log-2026-09-06.txt">Original snapshot</a>,
lines 1018–1039. The heading supplies the recorded date.

<!-- BEGIN IMPORTED BODY -->

- The next signed four-Kitty attempt crossed the repaired sandbox boundary and
  presented the startup Kitty. `Super+Enter` committed `LaunchTerminal`,
  started the second Kitty, observed its surface, and continued clean native
  page-flip retirement. Concurrent surface generations then made the xmonad
  layout proposal stale. Three such ordinary races each terminated the whole
  compatibility bridge, exhausted the supervisor's crash budget, and ended the
  session. This was not a frame-slot failure.
- Engine already re-arms `RejectedStale` and `TimedOut` outcomes and publishes a
  fresh snapshot. The bridge now drops its stateful private xmonad adapter,
  receives that snapshot on the same authenticated policy connection, and
  rebuilds the adapter from committed state. This preserves the rule that
  speculative xmonad state never survives a noncommitted proposal without
  misclassifying a recoverable scene race as an outer process crash.
- `RejectedInvalid` and `Disconnected` remain fatal. A decision-table
  regression covers every wire outcome. The configured conformance corpus now
  holds one authenticated bridge connection across timeout and stale outcomes,
  observes two private-adapter rebuilds, and commits both successor scenarios;
  the invalid scenario still requires a new outer process. A new signed
  four-Kitty run remains required.

<!-- END IMPORTED BODY -->

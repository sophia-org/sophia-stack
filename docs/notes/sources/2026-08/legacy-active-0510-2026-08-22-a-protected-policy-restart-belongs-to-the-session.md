---
id: legacy-active-0510
date: 2026-08-22
recorded_date: 2026-08-22
date_basis: heading
imported: 2026-09-06
kind: source
status: historical
tags: ["historical", "session", "policy", "security"]
---
# 2026-08-22: a protected policy restart belongs to the session

Historical source, not a current status claim. <a href="../../../history/research-log-2026-09-06.txt">Original snapshot</a>,
lines 15618–15643. The heading supplies the recorded date.

<!-- BEGIN IMPORTED BODY -->

Signed source `e946cc725bf731515a477c86e9a575554965418c` produced and
independently re-verified mirror archive `0007` and mixed archive `0001`. The
first run retained independently paced heads, primary-owned Present completion,
and last-head release. The second retained those rules beside the extended
output, exercised output-local submission ownership, and finished cleanly.
Those archives promote the per-head pacing row.

The following Hagia and metadata-broker gate stopped before Kitty appeared.
Bubblewrap cleared the old restart wrapper's `SOPHIA_HAGIA_BIN` variable and
mounted a private `/tmp`; the wrapper also expected the host evidence log and
restart marker there. Binding those paths or restoring ambient variables would
have weakened the policy domain to preserve a stale test harness.

The replacement keeps restart authority in Sophia. A bounded CLI proof control
arms only when its named public-policy action commits. It records the private
checkpoint file's device and inode, sends the ordinary committed outcome, and
requests one supervised replacement only after Hagia's atomic checkpoint write
changes that identity. It never reads checkpoint contents. The physical gate
now launches the real Hagia executable directly, and its verifier requires the
arm, nonempty checkpoint, trigger, epoch-two restart, load, reconciliation, and
post-restart action sequence in order. This is a local executable correction;
the complete three-gate sequence must bind its next signed candidate before the
protected broker row is promoted.

<!-- END IMPORTED BODY -->

# Sophia X11 Compatibility Matrix

**Role:** current native-X compatibility evidence and admission record.

This matrix is the admission record for the **Sophia X Server Frontend**. It
answers a deliberately narrow question: which real X11 client behavior has a
reproducible, bounded proof through Sophia's own X11 server frontend?

It is not an Xorg/XLibre conformance claim. A `proven` entry means the listed
probe completes its defined proof window with `first_error=none`; it does not
imply that nearby X11 requests, extensions, window-manager conventions, or
unlisted application modes work. `startup only` means the application reaches
the stated protocol stage without a client-visible X error but has no rendered
desktop proof yet.

The native frontend is always the subject of this matrix. XLibre is a retired
prototype and deferred possible provider; its behavior cannot promote an entry
here.

## Evidence Levels

| Level | Meaning |
| --- | --- |
| `wire` | Deterministic decoder/dispatcher fixture proves one bounded X11 request, reply, event, or error shape. |
| `client` | A real client completes its bounded protocol proof with `first_error=none`. |
| `engine` | The real client produces a reduced Sophia transaction that commits through Engine state. |
| `hardware` | A bounded one-client proof reaches Engine-owned native presentation and physical input, but does not yet meet the full session-promotion gate. |
| `session` | The client reaches normal startup, routed input, resize, presentation, and teardown backed by Sophia Engine and KMS. |

The paired Milestone 3 two-xterm entry meets the complete CPU-backed `session`
promotion gate under both classic-shared and fresh confined profiles. It is the
current baseline for physical input, Engine-derived output, RandR updates,
configure-plus-pixels resize, KMS presentation, and teardown. Other entries
retain only their listed evidence level. In particular, standard DRI3/Present
`vkcube` transport is `engine` evidence until the imported Vulkan pixels pass
mixed composition, page-flip-driven feedback, and the full session gate.

## Baseline Matrix

| Client or class | Reproducible command | Proven X11 surface | Evidence | Current status and next gate |
| --- | --- | --- | --- | --- |
| Setup parser and admission | `cargo test --offline -q -p sophia-x-authority --test x11_wire` plus `cargo test --offline -p sophia-session --features native-session live_xauthority_file_is_owner_only_valid_and_removed_on_drop -- --exact` | byte order, bounded setup fields, setup success/failure encoding, MIT-MAGIC-COOKIE-1 gating/provenance, peer-credential denial, concurrent identity allocation, revocation on disconnect/error/supervisor command, peer-preserving cleanup, and owner-only Xauthority removal | `wire` | `proven`; each live classic session publishes a fresh cookie and requires same-UID peers, while the live launcher can explicitly allocate a confined group. Independently credentialed groups on one listener remain before a general local session. |
| Raw core protocol | `cargo run --offline -q -p sophia-cli -- x-authority-x11-smoke` | atoms, property read/write, create/map window, core events | `client` | `proven`; grow only from a captured missing request. |
| Rust X11 client | `cargo run --offline -q -p sophia-cli -- x-authority-x11rb-smoke` | client-compatible setup, root/visual facts, atom/property/window flow | `client` | `proven`; setup assigns disjoint client XID ranges, concurrent classic clients retain same-namespace access, and policy-assigned confined clients reject foreign map, property, selection-owner, selection-requestor, and event-mask operations without metadata leakage or input redirection. Core grabs, XGE/XI2 delivery, the XI1 `ListInputDevices` enumeration projected from the same virtual master pair XI2 reports, XKB map/state/name, and deterministic parent/stack-based focus propagation are implemented; retained scene-restack evidence remains open. |
| Core selections and portal mediation | `cargo test --offline -q -p sophia-x-authority --test x11_wire cross_namespace_executor_installs_property_and_notifies_requestor -- --exact` plus `cargo test --offline -q -p sophia-portal --test socket` | same-namespace `PRIMARY`, owner replacement and `SelectionClear`; cross-namespace `CLIPBOARD`, authority-private source proxy, broker grant/payload correlation, requestor property install, `TARGETS`, UTF-8 text, and native failure notification | `wire` | `proven`; allowed, denied, stale, expired, disconnected, unsupported, and executor-failure paths are bounded and fail closed. Large `INCR`, XFixes selection notifications, and Xdnd remain later compatibility work. |
| `xdpyinfo` | `cargo run --offline -q -p sophia-cli -- x-authority-xdpyinfo-smoke` | root screen, extension discovery, root properties, focus, GC cleanup | `client` | `proven`; do not turn its empty extension list into an extension-completeness claim. |
| Minimal Xlib lifecycle | `cargo run --offline -q -p sophia-cli -- x-authority-xlib-smoke` | normal Xlib create/property/map/destroy path | `client` | `proven`; retain as the first C/Xlib regression. |
| Core drawing | `cargo run --offline -q -p sophia-cli -- x-authority-xlib-drawing-smoke` | GC lifecycle and `PolyFillRectangle` reduced to a ready transaction | `engine` | `proven`; visual output still needs session evidence. |
| Software image upload | `cargo run --offline -q -p sophia-cli -- x-authority-xlib-put-image-smoke` | bounded `PutImage` CPU-buffer update and ready transaction | `engine` | `proven`; retain buffer ownership/release requirements when real SHM import arrives. |
| Fixed TrueColor rendering | `sophia-truecolor-proof`, then `sophia-verify-truecolor-runs 1` | depth-24/bpp-32 setup, `AllocColor`, `AllocNamedColor`, `QueryColors`, exact asymmetric RGB/CMY/gray `PutImage`/`GetImage`, Kitty 24-bit DMA-BUF color, Engine composition, and KMS retirement | `hardware` | `proven` by immutable attempt `0003` from `883666a2`: all seven palette populations remained exact in the final primary-output region, Kitty retained every RGB/CMY channel, both measured regions causally preceded output-1 native submission and retirement, output 2 retained its nonzero startup baseline, and normal logout restored the TTY cleanly. The archive passed directly with `reverified=0`. This does not claim active client projection onto output 2. |
| Historical private explicit handoff | `cargo run --offline -q -p sophia-cli -- x-authority-present-pixmap-smoke` | `SOPHIA-PRESENT` query and bounded pixmap transaction | `engine` | `proven` only as a retained prototype regression; standard DRI3/Present supersedes it and no application promotion may depend on it. |
| Root inspection | `cargo run --offline -q -p sophia-cli -- x-authority-xwininfo-root-smoke` | root attributes, geometry, tree, coordinate translation | `client` | `proven`; live setup and later topology facts are Engine-derived, while the paired Milestone 3 row carries the dynamic RandR and resize session evidence. |
| Root properties | `cargo run --offline -q -p sophia-cli -- x-authority-xprop-root-smoke` | root property enumeration and bounded reads | `client` | `proven`; broaden only when a client captures a required atom/property pattern. |
| Root mutation | `cargo run --offline -q -p sophia-cli -- x-authority-xsetroot-name-smoke` | root name/property mutation, focus, GC, extension-query flow | `client` | `proven`; no Engine pixels are expected. |
| Simple Xaw drawing | `cargo run --offline -q -p sophia-cli -- x-authority-xlogo-smoke` | create/map/property plus polygon/rectangle drawing | `engine` | `proven`; use as a low-complexity drawing regression. |
| Dialog/message drawing | `cargo run --offline -q -p sophia-cli -- x-authority-xmessage-smoke` | message-window lifecycle and reduced drawing | `engine` | `proven`; widget/toolkit coverage remains limited. |
| Xaw widgets | `cargo run --offline -q -p sophia-cli -- x-authority-xcalc-smoke` | colors, unmap, padded text, normal disconnect cleanup | `engine` | `proven`; grow through xcalc traces rather than generic Xaw support. |
| Clock drawing | `cargo run --offline -q -p sophia-cli -- x-authority-xclock-smoke` | fonts, pixmaps, copy/draw damage, exposure | `engine` | `proven` for the probe window; not proof of every font or drawing path. |
| Eye drawing | `cargo run --offline -q -p sophia-cli -- x-authority-xeyes-smoke` | `QueryColors`, clear, fill-arc draw damage | `engine` | `proven`; colormaps remain reduced. |
| Output query | `cargo run --offline -q -p sophia-cli -- x-authority-xrandr-query-smoke` | bounded RandR version/output query | `client` | `proven` for populated snapshot-derived CRTC/output/mode/monitor facts and acknowledged, mask-selected dynamic screen/CRTC/output/resource notifications; live sessions can inject a validated deterministic size update without physical hotplug. |
| xterm startup | `cargo run --offline -q -p sophia-cli -- x-authority-xterm-smoke` | core setup/lifecycle and compatibility request trace | `client` | `proven` as a lifecycle proof; it intentionally does not require a committed render transaction. |
| xterm render | `cargo run --offline -q -p sophia-cli -- x-authority-xterm-render-smoke` | text drawing becomes a changing CPU-buffer transaction | `engine` | `proven`; retain as the focused software transaction probe, while the paired Milestone 3 row carries resize, presentation, and teardown evidence. |
| xterm input | `cargo run --offline -q -p sophia-cli -- x-authority-xterm-input-smoke` | bounded core key events make the proof shell receive exactly `sophia` and advance xterm CPU-buffer generation | `engine` | `proven`; live Engine input crosses as a routed surface request and is translated by the session RMLVO. Core and XKB maps share that snapshot; GetState/GetNames and selected StateNotify delivery are implemented. The paired Milestone 3 row carries physical multi-client input and focus evidence. |
| Two-client xterm routing | `cargo run --offline -q -p sophia-cli -- x-authority-xterm-two-client-smoke` | two real xterms receive distinct client-targeted core keys through the bounded concurrent frontend and each advances CPU pixels | `engine` | `proven` for brokered CPU-buffer routing and service drain; it is not KMS-backed multi-app session evidence. |
| Guarded one-client xterm | `tools/live_session_persistent_hardware_proof.sh` and `tools/live_session_content_hardware_proof.sh` | real xterm pixels reach Engine-owned GBM/KMS presentation; focused physical input changes later pixels | `hardware` | `proven` at the fixed established buffer size; normal resize and the full multi-client session contract remain open. |
| Guarded two-xterm session | `tools/live_session_two_xterm_hardware_proof.sh` | focused-terminal input plus a supervised second xterm flush every routed X11 event, produce later CPU pixels, and reach KMS presentation with at least two CPU layers | `hardware` | `proven`: retained run completed in 1,487 ms with 10 ms maximum composition, 23 ms input-to-presentation, all 14 events flushed, and clean KMS teardown. This earlier gate is retained but superseded for promotion by the paired Milestone 3 session proof. |
| Paired Milestone 3 session | `tools/live_session_milestone3_hardware_proof.sh` | run guarded two-xterm KMS evidence under classic shared-X and a fresh zero-capability confined namespace, including authenticated RandR delivery, configure-plus-pixels resize, exact physical keyboard input, and pointer-driven pixels | `session` | `proven`; retained schema-13 X13 runs passed strict paired verification with two CPU layers, all routed events flushed, physical text and pointer pixel changes, four RandR notifications, committed resize pixels, clean KMS teardown, 94/90 ms classic/confined startup readiness, 13 ms maximum composition, and 0 ms measured input-to-presentation latency. |
| GTK software dialog | Engine smoke plus `tools/qemu_milestone5_acceptance.sh` | GTK negotiates XKB/XGE/XI2, reads bounded RandR output properties, subscribes to XFixes selection changes, commits nonzero `MIT-SHM` pixels, accepts exact virtio text and an OK-button click, redraws after resize, and exits normally | `session` | `proven`: the unattended classic and confined QEMU profiles pass exact text, presented centered cursor, button-gated Return, committed 640x360 CPU/SHM resize redraw, normal exit, native two-output presentation, and clean retirement. The strict three-class verifier passes with retained xterm and Vulkan session evidence. Direct hardware runs are optional compatibility diagnostics. |
| Firefox native-X trace and native session | `cargo run --offline -q -p sophia-cli -- x-authority-firefox-smoke`, `sophia-firefox-proof`, and `sophia-verify-firefox-runs 1` | bounded native-X startup plus keyboard, pointer, navigation, resize, refocus, dialog, selection, close, and cleanup under native Hagia/Narthex policy | `session` | `proven` for the standalone 396-request, 45-opcode trace and the archived interaction/lifecycle contract. Current visible rendering is `implemented; physical requalification pending`: the last candidate reached page readiness and exact retirement but produced a black content region. Source-identity and child-Present regressions now pass, and the installed canary requires two changing nonblack regions tied to exact native frame retirement. No new session result is claimed until that signed canary passes. Historical bridge-era runs are not product gates. |
| Vulkan DRI3/Present transport | `cargo run --offline -q -p sophia-cli -- x-authority-vkcube-smoke` | Mesa RADV negotiates DRI3 1.2 Open/modifiers/multi-plane pixmaps, xshmfences, XFIXES regions, and standard Present | `engine` | `proven` on X13 for a bounded 68-request trace with three imported pixmaps and fences, one accepted Present transaction, one committed runtime surface, and `first_error=none`; it does not prove imported Vulkan pixels in the persistent renderer or native KMS page-flip feedback. |
| Vulkan deferred-map admission | `cargo run --offline -q -p sophia-cli -- x-authority-vkcube-admission-smoke` | a real policy-managed vkcube remains unmapped until generic admission, then continues DRI3/Present with exact feedback | `engine` | `implemented; host proof pending`: the routed gate requires map intent, delivered `AdmitSurface`, at least one DMA-BUF import, and two Complete/Idle round trips. It neither invokes a WM nor proves visible KMS pixels. |
| Kitty direct GLX bootstrap | `cargo run --offline -q -p sophia-cli -- x-authority-kitty-smoke` | GLVND selects Mesa; Kitty receives ARGB+sRGB FBConfig, creates direct contexts/windows, imports a depth-32 DRI3 buffer, submits Present, and tears down Sync fences/colormaps | `engine` | `proven` on the local AMD render node for a bounded 200-plus-request trace with one accepted Present transaction, one committed runtime surface, every required direct-GLX stage present, and `first_error=none`. The smoke terminates after its first-frame proof; guarded TTY3 input, normal logout, and recovery remain the session gate. Indirect GLX is deliberately unsupported. |
| Classic direct GLX bootstrap | `cargo run --offline -q -p sophia-cli --features native-session -- x-authority-glxgears-smoke` | depth-capable double-buffered visual selection, legacy direct-context creation, DRI3 import, and Present submission | `engine` | `proven` by the bounded real-Mesa preflight with every required wire stage and no X protocol error. Direct Mesa does not send MakeCurrent over the wire for this workload; Sophia nevertheless implements validated legacy MakeCurrent for clients that do. Physical animation and KMS cadence remain pending. |
| GLX offscreen drawables | `cargo test --offline -q -p sophia-x-authority --test x11_wire glx_pbuffer` | GLX 1.3 pbuffer create and destroy, core `GetGeometry` on a GLX drawable, intrinsic drawable attributes, `MakeContextCurrent` | `wire` | `proven` at the wire and dispatch boundary, plus a real-Mesa offline probe. GLX 1.3 minors 13, 15, 22 and 23 (GLX pixmaps), 25 `QueryContext` and 30 `ChangeDrawableAttributes` stay unimplemented and answer `BadRequest`; `GLX_PIXMAP_BIT` is withdrawn so the advertised drawable types are exactly the implemented ones. Proven against real Mesa by `cargo run --offline -q -p sophia-cli --features native-session -- x-authority-glx-pbuffer-smoke`, which drives `pbdemo` through a GLX pbuffer and exits zero with `first_error=none`. The feature is required, not decorative: without it `external_probe_pixmap_allocator` is compiled out to `None`, the server-owned half of DRI3 never runs, and the recovery refuses a pbuffer it should have backed -- a refusal that reads as a product defect and is not one. The run stays `wire` rather than `engine` because it commits no transaction (`transactions=0`); an offscreen pbuffer has no surface to commit. The GLX 1.3 request surface is complete apart from GLX pixmaps (13, 15, 22, 23), which are withdrawn rather than promised; the indirect-GLX requests (1, 2, 8, 9, 10, 11, 12, 16, 17) are excluded by architecture, since Sophia renders client-side and imports through DRI3. GLX 1.4 adds only multisample attributes over 1.3, and those are advertised. Both causes previously named outside GLX are closed: DRI3 minor 8 `BuffersFromPixmap` answers from retained plane descriptors, and `GLX_EXT_create_context_es_profile` and `GLX_EXT_create_context_es2_profile` are advertised for a translating client's fallback. |
| Kitty interactive session | `cargo run --offline -q -p sophia-cli -- x-authority-kitty-input-smoke` plus `tools/start_sophia_kitty_tty3.sh` | real Kitty GLX/DRI3 startup, routed US keyboard and pointer input, Present feedback, native two-output scanout, normal exit, and TTY/display-manager restoration | `session` | `proven` with installed Kitty 0.48.0 and the guarded 2026-07-24 TTY3 run: the automated gate routes ten core key events, receives exact `:ll` including Shift+semicolon XKB state, and observes three post-input Presents; the physical run reached readiness in 798 ms, routed keyboard and two button transitions, bounded cursor motion-to-submit at 13 ms, exited Kitty with status zero, retained clean protocol health, and restored the originating TTY without emergency recovery. Traditional extension ranges are non-core and mutually disjoint. This promotes only the Kitty baseline session. |
| xmobar X11 application trace | `cargo run --offline -q -p sophia-cli -- x-authority-xmobar-smoke` | an override-redirect X11 application remains client-positioned and produces Engine-owned CPU-buffer transactions | `engine` | `proven` for the bounded application trace: xmobar 0.51.1 completed 215 requests across 28 opcodes with six committed transactions and nonzero pixels. This is X Authority application compatibility evidence, not a packaged shell, WM-policy path, or native shell qualification. |
| XF86VidMode modeline | `cargo test --offline -q -p sophia-x-authority --all-features --test x11_wire vidmode` | `QueryVersion` answers 2.2, `GetModeLine` returns the scanout timing DRM reported, and `SetClientVersion` is accepted because `libXxf86vm` sends it next | `wire` | `proven` at the dispatch boundary. Mesa implements `glXGetMscRateOML` through this extension, so every GL client asked and was refused: Brave logged the failure and Xlib's own `XMissingExtension` once per frame, and Chromium paced frames against an assumed refresh rate. The reply carries measured timing rather than the nominal rate -- a panel advertised at 120 Hz runs at 119.997 Hz, and `glXGetMscRateOML` returns a numerator and denominator precisely so that survives. The nominal `refresh_millihz` is unchanged and still what a profile's `@120` matches; a test holds that line, because conflating the two would stop `mode "2560x1440@120"` matching anything. An output with no measured timing is refused rather than answered with a guess. Mode switching, gamma and viewport are declined by name: Sophia owns modesetting. It stays `wire` rather than `client` because the headless probe does not reach the call -- Qt queries the rate only against a real display. |
| Deliberately excluded extensions | `QueryExtension` refusal logging (`sophia_x11_authority_extension schema=1 status=absent`) | `Composite`, `DAMAGE`, `XTEST`, `DPMS` and `XFree86-Bigfont`, each absent by decision rather than by oversight | `decision` | Not a gap. Each names a domain Sophia owns, and the reasoning is recorded against a server that implements all of them: yserver runs whole desktops with Composite, DAMAGE, XTEST and DPMS, so the cost and the payoff are measured rather than guessed. **Composite** would delegate compositing, which Sophia does not delegate -- Hagia is the only window manager and no external compositor is planned; if a measured client ever needs it, the shape to adopt is yserver's overlay-window handover, where the scene emits only root, overlay and cursor once a compositor claims the overlay, and their staged path records redirects and answers `NameWindowPixmap` before allocating backings. **DAMAGE** serves the same external compositors and screen scrapers; if admitted, its reports must stay separate from internal repaint damage, which yserver keeps as distinct machinery on purpose. **XTEST** is four requests and cheap, and that is not the objection: yserver's is entirely ungated, which their own notes record as a known gap, and Sophia's input is session-owned authority, so synthetic input needs an admission story before it exists. Revisit when conformance tooling needs it. **DPMS** is session power authority, not authority-side connector writes. **XFree86-Bigfont** was surfaced by the xterm probe; yserver ships nothing and xterm works there, and Xlib falls back cleanly. Every one of these is revisited by observation: `QueryExtension` records what it refuses, so a real client asking changes the answer. |
| SHAPE window regions | `cargo run --offline -q -p sophia-cli --features native-session -- x-authority-quickshell-smoke` plus `cargo test --offline -q -p sophia-x-authority --all-features --test x11_wire shape` | all nine requests, the three shape kinds, the five combining operations, masks read from depth-1 pixmaps, change-gated `ShapeNotify`, and bounding and input shapes that take effect | `client` | `proven` for version 1.1. SHAPE was the last extension the absent-extension log still named, and the Quickshell trace now reaches opcode 145: 35 opcodes and 331 requests, up from 34 and 329, with the refusal line gone. **The advertisement waited for input shapes to work, not merely to be stored.** A Qt panel's first use of SHAPE is an input shape so clicks fall through the parts of its window that are not the panel; storing that while still swallowing the clicks would have been a silent lie to the one client measured asking for it, which is worse than the clean fallback of not offering the extension. Bounding shapes clip composition by publishing the presentation buffer as premultiplied ARGB with the pixels outside the shape cleared to transparent -- the renderer already blended that format and the buffer validator already accepted it, so no protocol or renderer change was needed. Transparent rather than black, because black would be the window painting over the desktop. Input shapes reach the engine through `LayerSnapshot.input_region` on the authority's own unversioned transport, and hit testing skips a layer whose shape excludes the point so the click reaches what is beneath; the policy protocol to the window manager is deliberately untouched, since a blind WM has no use for an input shape. Each kind is unset, explicitly empty, or concrete, and those are three different answers: an unset kind tracks the window's live geometry, so a resize moves it without anything writing the store. `ShapeNotify` fires only when the effective region moves -- window managers re-assert shapes constantly, and canonical region equality means the same area re-sent as four rectangles instead of one is correctly silent. `ShapeInvert` is the source with the destination removed, the mirror of Subtract, which at least one other implementation gets wrong by aliasing it to Set. Named limits: a descendant window's shapes are stored, answered and notified but do not yet clip the parent presentation or affect intra-toplevel routing; `ShapeClip` is stored and consumed by nothing, because Sophia composes whole client buffers and draws no window borders, so it could never differ visibly from bounding; a shaped window falls back to scaling its 1x raster on a non-1x display, as every composed result here does; and pointer grabs capture regardless of shape, which is what X defines. |
| RENDER compositing and glyphs | `cargo run --offline -q -p sophia-cli --features native-session -- x-authority-render-smoke` plus `cargo test --offline -q -p sophia-x-authority --all-features --test x11_wire render` | picture lifecycle, premultiplied Porter-Duff `Composite` and `FillRectangles`, repeating sources, masks including component alpha, and the antialiased glyph family | `client` | `proven` for version 0.5 by round trip rather than acceptance: a real x11rb client composites opaque red over half-alpha blue and reads back `[0, 0, 255, 255]`, then composites a two-pixel A8 glyph whose second pixel is half-covered and reads back `[0, 0, 128, 128]` -- the premultiplied scaling of partial coverage, which is the whole reason a toolkit wants this extension. Both Quickshell and Brave asked for RENDER and were refused before this; the absent-extension log is what put it on the list, and the Quickshell trace now grows from 33 opcodes and 326 requests to 34 and 329, with `RENDER:QueryPictFormats` asserted as a required stage so a regression in the advertisement cannot silently return the shell to core drawing. **The advertised version is 0.5 and it is exactly what is implemented.** MIT-SHM had advertised 1.2 with opcodes 6 and 7 missing, Qt believed it and fell back to the wire; here the advertisement was withheld through three commits until the requests behind it answered, because the base protocol carries no version gate and `QueryExtension` presence alone licenses `CreatePicture` and `Composite`. Refusals are two-tier and each names its minor: a request defined within 0.5 and not implemented answers `BadImplementation` -- the trapezoid family (10-13), declined because no measured client sends them and the refusal log will say if one appears, plus the five Xorg itself never wrote (`QueryDithers`, `Scale`, `ColorTrapezoids`, `ColorTriangles`, `AddGlyphsFromPicture`) -- while anything above 0.5 answers `BadRequest`, because a real server of that version had no dispatch entry for it. Operators 0-13 are implemented; the Disjoint, Conjoint and PDF ranges answer `BadImplementation` and values no version defines get the extension's own PictOp error. Alpha maps and pixmap clip masks are declined rather than ignored, since ignoring one changes what the client drew without saying so. Component-alpha masks are implemented rather than approximated: Xft under LCD filtering sends them, and treating one as a plain mask renders text with colour fringes that read as a display fault. Two honest limits: RENDER results have no journal representation, so a window they touch falls back to scaling its 1x raster on non-1x displays; and a depth-32 window drawn this way still ships to the compositor tagged `XR24`, the same degradation core drawing already has. `x-authority-xterm-render-smoke` is retained as regression evidence and still reports `ascii_marker_match=true` with `first_error=none` -- it launches with a core `-fn 6x13` font, so advertising RENDER does not divert its text path. ARGB cursors (minor 27) are **stored, not yet displayed**: `CreateCursor` keeps the picture's premultiplied pixels and hotspot, clamping each channel to its alpha at ingest so a stored image always satisfies the engine's `CursorAsset` validator, and `FreeCursor` releases them -- the smoke creates and frees one over a real connection. The cursor the compositor actually draws still comes from configuration through `resolve_cursor_theme`; the authority-to-engine-to-session plumbing that would put a client's cursor on screen is a named follow-up and is not claimed here. Oversize cursors are refused rather than scaled, because a silently resized cursor is one whose hotspot no longer points where the client put it. |
| XC-MISC identifier ranges | `cargo run --offline -q -p sophia-cli --features native-session -- x-authority-shm-fd-smoke` plus `cargo test --offline -q -p sophia-x-authority --all-features --test x11_wire xc_misc` | `GetVersion` answers 1.1 and `GetXIDRange` hands a client a further block of resource identifiers once the block it received at connection setup is spent | `client` | `proven` for the grant: the smoke asks over a real connection, receives 2,097,152 identifiers, and asserts the block lies outside the range that same client was given at setup. That non-collision is structural rather than asserted -- one monotonic counter in `x11_socket/state.rs` serves both connection setup and XID grants, so a grant cannot name a range any client holds. The two paths therefore draw from one finite pool of 2,047 ranges: a client that exhausts and re-asks repeatedly consumes ranges new connections would otherwise get, and when the pool is spent both refuse rather than overlap. `count=0` is a real protocol answer that clients handle by giving up cleanly, which is why the dispatch layer returns it as its default -- dispatch cannot see the counter, and inventing a range there would hand out identifiers belonging to another client. Released identifiers are never recycled; a session long enough to spend four billion of them is not one this reclaims. `GetXIDList` is bounded at 4,096 so a `CARD32` count cannot ask the server to build a four-billion-word reply. Xlib, XCB and x11rb all call this on exhaustion, and until now the extension was absent, so a long-lived client that churned resources had no way to continue. |
| MIT-SHM 1.2 descriptor segments | `cargo run --offline -q -p sophia-cli --features native-session -- x-authority-shm-fd-smoke` | `CreateSegment` allocates and returns a descriptor, `AttachFd` accepts one from the client, and both directions cross the socket's `SCM_RIGHTS` path | `client` | `proven` by a round trip rather than by acceptance: sixty-four bytes written through the descriptor the server returned are read back through the server's own mapping, so the descriptor names the memory the server holds. A `CARD32` size beyond what the adapter maps is refused rather than allocated. Segments the server allocates are sealed against resizing, which is what keeps a later read from becoming a `SIGBUS` when the holder truncates the file. `ShmQueryVersion` had advertised 1.2 while only opcodes 0-5 were implemented; a Qt shell believed it, called `xcb_shm_create_segment`, and fell back to the wire. `x-authority-quickshell-software-smoke` exercises the software path but does not prove the segment requests: Qt allocates no backing store until something exposes its window, and offline nothing maps a dock. |
| Quickshell shell panel trace | `cargo run --offline -q -p sophia-cli --features native-session -- x-authority-quickshell-smoke` | a Qt6/QtQuick shell brings up a `_NET_WM_WINDOW_TYPE_DOCK` panel with `_NET_WM_STRUT_PARTIAL`, reaching GLX context creation and DRI3 `PixmapFromBuffers` | `client` | `proven` for a bounded 326-request trace across 33 opcodes including DRI3 and Present, with no server-caused refusal. It commits no transaction and stays `client` for that reason: a dock is not mapped until a policy client admits it, and this probe has no window manager -- admission is the session gate's to prove, as it is for vkcube. One client-caused refusal is tolerated by name: Qt destroys and recreates its window when the format changes, and Mesa's DRI3 teardown then unselects Present events on the destroyed id three requests later, which Xorg refuses identically. A live session first showed this as nine `major=138 minor=0` refusals, which named `QueryVersion` because the minor opcode was not carried; that is fixed and the trace now names `SelectInput`. |
| Vulkan mixed native session | `tools/live_session_milestone4_hardware_proof.sh` | paired software xterm/resize and `vkcube` plus CPU-layer native sessions; controlled acquire delay, rejection recovery, mixed export, page-flip Complete/Idle, idle fence, and exact teardown | `session` | `proven` on X13: the retained schema-14 GPU run completed 76 mixed Flip events, one controlled Skip, 77 matching Idle events and idle-fence triggers, nine acquire-gate waits, zero submit/retire failures, and zero live resources. The paired software xterm/resize baseline also passes. |
| Window manager advertisement | `cargo test --offline -q -p sophia-x-authority --test x11_wire a_client_asking_whether_a_manager_runs_is_answered -- --exact` plus `cargo run --offline -q -p sophia-cli --features native-session -- x-authority-browser-smoke` | root `_NET_SUPPORTING_WM_CHECK` and `_NET_SUPPORTED`, the authority-owned check window at `0x24`, its self-reference, and its `_NET_WM_NAME` | `wire` | `implemented; physical proof pending`: Sophia previously answered no root property but `RESOURCE_MANAGER`, so every EWMH client concluded no window manager was running while Hagia and Sophia configured and placed its windows underneath it. The offline browser probe recorded Helium reading `_NET_SUPPORTING_WM_CHECK` and `_NET_SUPPORTED` off the root and receiving nothing; it now completes the handshake with `first_error=none` and issues three more requests. It does not yet map a window in that probe, which stalls before its GPU process starts, so this does not claim the blank-window cause is fixed. `_NET_SUPPORTED` lists only hints with behaviour behind them: the `_NET_WM_STATE` set, both struts, `_NET_WM_WINDOW_TYPE`, and `_NET_WM_NAME`. `_NET_ACTIVE_WINDOW`, `_NET_CLIENT_LIST`, `_NET_CURRENT_DESKTOP`, `_NET_FRAME_EXTENTS`, `_NET_WM_SYNC_REQUEST` and `_NET_WM_MOVERESIZE` are withheld though clients do ask for them, because Hagia is blind and would not honour them. `ReparentNotify` remains unimplemented. |
| Cross-process Present | `cargo test --offline -q -p sophia-x-authority a_present_from_a_client_that_did_not_create_the_window_is_admitted -- --exact` | a present admitted for a window the presenting client did not create, its feedback delivered to the subscribing client, a refusal answered as a client-visible error, and Present `NotifyMSC` | `wire` | `implemented; physical proof pending`: Present admission previously required the presenting client to be the window's creator, and a failure ended the connection reader with no client-visible error and no EOF -- the writer half held the socket open, so the client waited on a conversation nobody read. A single-connection client cannot observe either fault, which is why kitty, glxgears, vkcube and Firefox pass throughout; a browser that presents from a GPU process to a window its browser process created is killed silently. `NotifyMSC` was an undecodable request while Present 1.2 was advertised, and Mesa blocks on the completion it asks for. The clock it answers from is the last completion's ust/msc, so a target at or behind it answers at once and one ahead waits for a completion to advance it. Present minors with no implementation now answer `BadImplementation` rather than failing to parse. This does not claim the browser renders; that is the physical gate's to prove. |

## Admission Rule

Every native X11 change must update this matrix when it changes a real-client
result. The change should include:

1. The exact client, command, environment precondition, and version if it
   affects request behavior.
2. The first missing request, reply, event, or extension fact, captured in
   bounded trace data without publishing client metadata or payloads.
3. A focused wire/authority regression plus the corresponding real-client
   command. The client output must retain `first_error=none`.
4. The smallest matrix row or row update that states both what is proven and
   what is deliberately not proven.

An unsupported request must remain a normal client-visible X11 error. Do not
advertise an extension merely to advance a probe, add unrelated Xorg behavior
speculatively, or route raw X11 state into Sophia Engine.

## Promotion Gate

An application class can move from `engine` to `session` evidence only when a
real client proves all of the following through the native frontend:

- normal startup and teardown;
- configured size and output facts derived from Engine rather than fixed setup
  constants;
- Engine-selected keyboard and pointer delivery, including the applicable X11
  focus/grab semantics;
- buffer readiness, delayed release, and presentation feedback on the chosen
  SHM or DRI3/Present path;
- committed Sophia Engine state and real KMS presentation; and
- the applicable classic shared-X or confined-namespace policy.

Until then, unsupported applications remain unsupported by the native session.
Reconsider an optional XLibre provider only after measured compatibility gaps
justify its authority and maintenance cost.
